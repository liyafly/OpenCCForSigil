{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OpenCCForSigil Profile",
  "type": "object",
  "required": ["schema_version", "id", "conversion", "segmentation"],
  "properties": {
    "schema_version": {"const": 1},
    "id": {"type": "string", "minLength": 1},
    "conversion": {"type": "string"},
    "segmentation": {"enum": ["mmseg", "jieba"]},
    "builtin_rules_enabled": {"type": "boolean"}
  },
  "additionalProperties": true
}

# 2026-09-08 未发布改动审查

审查基线：`79d2ec4`（v0.0.2-beta），范围为其后的 UI、文件计数、文档打包和许可证改动。

## 结论与修正

- 独立审查未发现本次 UI/core 改动引入的转换正确性缺陷。分词缓存仅在源字符串一致时复用；暂存、结构检查和写回前源哈希检查仍保留。
- 结果将分析文件数、实际写回文件数和没有建议变更的文件数分别显示。38 个目标中仅 37 个需要写回不代表第 38 个失败；具体用户书籍仍须结合实际变更确认。
- 修正 UI 方案的完成状态，避免把尚未实施的分组预览、返回设置和完整宿主验收称为已完成。
- 发布文档区分 CI 提交哈希文件名、Release 版本文件名、历史发布附件和 GitHub 自动源码归档。
- 包校验要求项目 LICENSE/NOTICE 及 cppjieba 许可证存在，测试直接验证构建后的归档和缺失许可证时的拒绝行为。

## 验证范围

`make check` 通过：59 项测试、Ruff、载荷校验、OpenCC 差分 16/16、Jieba 差分 10/10。
`mise exec -- uv lock --check` 通过；本机插件 ZIP 构建与最终归档校验通过。
发布 shell 语法和模拟 gh 参数验证通过，未调用真实发布接口。文档本地链接检查通过。
本地载荷为 macOS arm64/cp314；完整四平台发布矩阵仍由后续 GitHub Actions 执行。

本次未运行真实 Qt 大书性能测试。单文件 native 转换、暂存字符串拼接、预览最终确认及写回仍可能产生同步等待；不能把进度窗口更早显示等同于所有性能问题已解决。
本记录不宣称真实 EPUB、跨平台宿主或阅读器验收通过。

# Privacy defaults

Session logs record structured events, changed tokens, file/position metadata,
and short diagnostic context when those features exist. The default does not
save an entire EPUB or an entire text node. User data belongs under the Sigil
support directory when available, with a home-directory fallback.

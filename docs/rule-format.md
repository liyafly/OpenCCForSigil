# Rule format

User rules are a later-phase, directional overlay. A rule without an explicit
conversion direction does not exist. Official OpenCC data remains read-only;
user exact/protected rules will be applied through the locked-span model rather
than by modifying bundled dictionaries.
